# Migration `20201110031755-initial`

This migration has been generated by Brian at 11/9/2020, 9:17:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."UserRole" AS ENUM ('ADMIN', 'EDITOR', 'AUTHOR')

CREATE TABLE "public"."Post" (
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
"id" text   NOT NULL ,
"title" text   NOT NULL ,
"html" text   NOT NULL ,
"slug" text   NOT NULL ,
"likes" integer   NOT NULL ,
"authorId" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."User" (
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
"id" text   NOT NULL ,
"email" text   NOT NULL ,
"roles" "UserRole"[]  ,
"password" text   NOT NULL ,
"givenName" text   NOT NULL ,
"surname" text   NOT NULL ,
"avatar" text   NOT NULL DEFAULT E'https://www.gravatar.com/avatar/205e460b479e2e5b48aec07710c08d50',
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

ALTER TABLE "public"."Post" ADD FOREIGN KEY("authorId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201110031755-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,45 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgres"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+
+
+model Post {
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  id        String   @id @default(cuid())
+  title     String
+  html      String
+  slug      String
+  likes     Int
+  author    User     @relation(fields: [authorId], references: [id])
+  authorId  String
+}
+
+model User {
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  id        String   @id @default(cuid())
+  email     String   @unique
+  roles     UserRole[]
+  password  String
+  givenName String
+  surname   String
+  // Get sizes by adding ?s=200
+  avatar    String   @default("https://www.gravatar.com/avatar/205e460b479e2e5b48aec07710c08d50")
+  posts     Post[]
+}
+
+enum UserRole {
+  ADMIN
+  EDITOR
+  AUTHOR
+}
```


